학습 목표
    - CSS 뜻에 대해 알아보자

    - CSS의 뜻
        CSS를 구글링을 해보면 순차적으로 내려오는 폭포, 계단을 확인 할 수 있다.
        이처럼 브라우저가 CSS코드를 읽을 때 위에 있는 코드부터 차례대로 읽는다는 뜻이다.

    - CSS의 뜻을 안뒤 효용
        CSS를 HTML 에 적용하는 방법은 두가지 존재했다.
        inline 방식과 external 방식
        이 두 방식을 이용하여 코드를 작성하면 inline 방식이 먼저 오는지, external방식이 먼저 
        오는지에 따라 다르게 보인다. 즉, 코드의 순서가 영향을 미친다는 것을 알 수 있다.